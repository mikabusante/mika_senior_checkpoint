import { SET_CAMPUSES, SELECT_CAMPUS, ADD_CAMPUS } from './constants';

const initialState = {
  campuses: [],
  selectedCampus: {}
};

export default (state = initialState, action) => {
  //your code here
  return state;
};
